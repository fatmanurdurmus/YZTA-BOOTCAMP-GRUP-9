# Shared Schemas

This package is reserved for generated or shared schema contracts between the FastAPI backend and the React frontend.

Sprint 1 source of truth is the Pydantic schema layer under `apps/backend/src/carbonpilot/schemas`.
