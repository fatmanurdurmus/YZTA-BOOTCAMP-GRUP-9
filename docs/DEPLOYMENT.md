# Deployment Guide

## Frontend (Vercel)

The frontend lives in `apps/frontend` inside this monorepo, so Vercel needs
to be told where to look.

1. Import this GitHub repo into a new Vercel project.
2. In **Project Settings → General → Root Directory**, set it to: